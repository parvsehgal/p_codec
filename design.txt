initial first steps{
  get a raw image as input
  get its Height and Width
  and read all of its RGB pixel values in a char buffer
}

next couple of step{
  do chroma subsampling of the image (convert it to Y:Cr:Cb either 4:2:2 or 4:2:0)
}


do need this application to be a CLI


-----------------------------------------------------------------------------------------------------------------------------

broken down steps
  initial read and conversion to YCRCB -> class name chroma

  DCT ,quantization -> dct 

  zigzag and entropy coding -> class name to be decided could be entropy idk ?


---CHROMA class

  goal is to get height , width , and path to raw image then return a yuv420p buffer on which DCT can be applied cleanly this buffer will be passed to another DCT class


---DCT class

take the sumSample buffer as input split into 8x8 blocks and perform DCT and quantization


--Entropy class

take in quantized coefficients arrage in zig-zag pattern and apply run level coding /huffman coding{
  send the run level pairs of each 8x8 section and pass them to a function that uses the bitwriter class to append the bits to its own stream
  after every 8x8 block has been added to the bitstream then the flush function should be executed and the its bitstream should be moved to a returnable variable this should give use the final compressed file

  should be able to start wroking on a decoder
}


--------------------------------------------------------entropy
if get time setup inverse DCT to check if it can be converted back to yuv420 that works  
